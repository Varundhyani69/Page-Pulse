# Base URL of the Page Pulse API.
# In development, the Vite proxy (vite.config.js) forwards /api/* to localhost:5000,
# so you don't need to set this during local dev.
# On Render (or any production host), set this to your deployed API URL:
VITE_API_URL=https://your-server.onrender.com
