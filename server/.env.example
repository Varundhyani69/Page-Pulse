# MongoDB connection string (get this from MongoDB Atlas or use a local URI)
MONGO_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/pagepulse?retryWrites=true&w=majority

# Port the Express server runs on
PORT=5000

# URL of the frontend (used for CORS)
CLIENT_ORIGIN=http://localhost:5173
